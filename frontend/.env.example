# 后端 API 地址（开发服务器代理使用）
# 开发模式下 SSR 直出品牌也读取本地址，请与实际后端端口保持一致
VITE_API_BASE_URL=http://127.0.0.1:8080

# 生产 SSR 进程访问后端拉取品牌配置的地址
# 可选，生产部署运行时读取，无需重新构建，也可写入部署根目录或入口向上任意一层的 .env，
# 默认 http://127.0.0.1:8080；拉取失败时 SSR 日志输出 [zonelease-ssr] 前缀警告
# SSR_API_ORIGIN=http://127.0.0.1:8080
